# v0.48 review target

The v0.48 merge gate requires the dedicated Windows/Linux workflow, legacy delegation compatibility, v0.47/v0.46 retention, canonical-context regressions, the full HomeServer Windows packaged executable and installer lifecycle, and post-merge verification on the exact main SHA. The implementation is not 10/10 until those exact-head checks are green.
